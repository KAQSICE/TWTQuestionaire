<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tk.tranced.twtform">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".paperlist.PaperlistActivity"></activity>
        <activity android:name=".editor.QuestionEditorActivity" />
        <activity
            android:name=".editor.QuestionTypeActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".editor.PreviewActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".newpaper.NewpaperActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".main.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>