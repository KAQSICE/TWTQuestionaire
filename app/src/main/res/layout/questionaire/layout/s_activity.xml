<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".star.StarActivity">

    <include
        android:id="@+id/star_toolbar"
        layout="@layout/common_toolbar" />

    <androidx.appcompat.widget.SearchView
        android:id="@+id/s_search"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/star_toolbar" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/s_button_chain"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/s_search">

        <ImageButton
            android:id="@+id/s_q1_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@null"
            android:src="@mipmap/questionaire"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toLeftOf="@id/s_v_button"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageButton
            android:id="@+id/s_v_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@null"
            android:src="@mipmap/vote"
            app:layout_constraintLeft_toRightOf="@id/s_q1_button"
            app:layout_constraintRight_toLeftOf="@id/s_q2_button"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageButton
            android:id="@+id/s_q2_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@null"
            android:src="@mipmap/quiz"
            app:layout_constraintLeft_toRightOf="@id/s_v_button"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />


    </androidx.constraintlayout.widget.ConstraintLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/s_paper_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/s_button_chain" />

</androidx.constraintlayout.widget.ConstraintLayout>