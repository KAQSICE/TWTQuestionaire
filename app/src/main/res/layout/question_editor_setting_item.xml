<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="14dp">

    <TextView
        android:id="@+id/questionEditorSettingLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#000"
        android:textSize="16sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/questionEditorSetting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:drawableEnd="@mipmap/editor_arrow"
        android:drawableRight="@mipmap/editor_arrow"
        android:drawablePadding="5dp"
        android:text="tetetea"
        android:textColor="#CCC"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="@id/questionEditorSettingLabel"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/questionEditorSettingLabel" />

</androidx.constraintlayout.widget.ConstraintLayout>